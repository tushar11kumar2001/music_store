class Singer {
    constructor(name, title, photo, audio) {
        this.name = name;
        this.title = title;
        this.photo = photo;
        this.audio = audio;
    }
}
export default Singer;
